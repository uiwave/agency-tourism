---
import Layout from "@/layouts/Layout.astro";
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const tours = await getCollection('tours');
  const enTours = tours.filter((tour) => tour.id.startsWith('en/'));
  
  return enTours.map((tour) => ({
    params: { id: tour.id.replace(/^[^/]+\//, '') },
    props: { tour },
  }));
}

const { tour } = Astro.props;
const { Content } = await render(tour);
---

<Layout title={tour.data.title}>
  <main>
    <h1>{tour.data.title}</h1>
    
    <ul>
      <li><strong>Destination:</strong> {tour.data.destination}</li>
      <li><strong>Duration:</strong> {tour.data.duration}</li>
      <li><strong>Type:</strong> {tour.data.type}</li>
      <li><strong>Price:</strong> ${tour.data.price}</li>
    </ul>

    <hr />

    <article class="prose prose-lg">
      <Content />
    </article>
  </main>
</Layout>
